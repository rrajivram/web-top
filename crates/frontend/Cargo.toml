[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.7", features = ["csr"] }
shared = { path = "../shared" }
serde_json = "1"
console_error_panic_hook = "0.1"
